# Moving Ball

Red ball (radius 25) on a white 800×600 canvas.

| Key | Action |
|-----|--------|
| ↑ | Move up 20 px |
| ↓ | Move down 20 px |
| ← | Move left 20 px |
| → | Move right 20 px |
| Esc | Quit |

Ball cannot move outside screen boundaries.

Run: `python main.py`
