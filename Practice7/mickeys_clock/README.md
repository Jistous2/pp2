# Mickey's Clock

Displays current system time with two rotating hands.

- **Right hand** = minutes
- **Left hand** = seconds

Place a `mickey_hand.png` image (pointing upward) inside `images/` for custom graphics. Without it, a simple grey hand is drawn automatically.

Run: `python main.py`
